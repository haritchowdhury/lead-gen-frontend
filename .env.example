# Server-only URL for the separate lead-generation backend. No trailing slash.
BACKEND_API_BASE_URL=http://127.0.0.1:3000

# Optional when the backend has no BACKEND_API_TOKEN configured.
BACKEND_API_TOKEN=replace-with-the-backend-service-token

